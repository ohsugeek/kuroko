# Kuroko（黒子）— ブランド・設計定義

Zoom等のWeb会議で、髪をリアルタイムに自然な黒髪へ再着色する常駐アプリ。
現実では自由に髪を染め、会議のときだけ黒髪で映ることを可能にする。

## 名称

- 正式名称: **Kuroko**（表記は英字。日本語文脈では「黒子（くろこ）」）
- 由来: 歌舞伎・文楽の「黒子」。黒をまとい自らは見えずに本番（＝あなたの実像）を成立させる裏方。
  「会議の見た目を肩代わりし、実生活のあなたを自由にする」という本アプリの本質と合致する。
- リポジトリ／識別子は小文字 `kuroko`。

## アイコン

- コンセプト: 黒子頭巾をかぶった人影のシルエット。「見られていないが確かにそこにいる」黒子性と、
  目もとの一線でカメラ（視線）を同時に表現。
- 構成: 生成り色の角丸地 + 墨黒のフードシルエット + 目もとに柿色の細いスリット1本。
- 正マスター: `assets/kuroko-icon.svg`（512px想定・viewBox 0 0 100 100）。
- 反転案（暗い地に生成りのフード）も可。タスクバー／トレイが暗い環境で映えるため用途により使い分ける。
- 配布用には SVG から `.ico`（16/32/48/256px マルチサイズ）を生成する。16pxでも輪郭が保たれることを確認済み。

## 配色 — 歌舞伎「定式幕（じょうしきまく）」由来

黒子が実際に立つ舞台の幕の三色（黒・柿・萌葱）を基礎に据える。意味的な一貫性のための選定。

| 名称 | ローマ字 | HEX | 役割 |
|---|---|---|---|
| 墨黒 | sumi | `#1A1512` | 署名色。髪の再着色色そのもの。アプリの基調 |
| 柿 | kaki | `#D65A2E` | 強調・CTA・アクティブ状態 |
| 萌葱 | moegi | `#2F6E4F` | 状態色（稼働中・成功）。暗背景では `#3E8E63` に持ち上げる |
| 生成り | kinari | `#F4F0E9` | 明色面・文字（暗背景上） |
| 鼠 | nezumi | `#8A857E` | 補助・境界・二次テキスト |

- アプリ本体は**ダークテーマ**を基調とする（黒を扱うアプリとして最も自然で、プレビューが映える）。
  - 画面背景 `#16130F` / サーフェス `#211D18` / 境界 `#322C25` / 本文 `#F4F0E9` / 二次テキスト `#A79F95`

### UI統一ルール（必須）

**GUIは全要素をKurokoブランドで統一する。標準OS/フレームワークの素の見た目を残さない。**

- 標準 `MessageBox` は使わない → テーマ付き `ConfirmDialog`（確認/通知）・`InputDialog`（入力）を使う
- WinForms の `ContextMenuStrip`（トレイメニュー等）は `KurokoMenuRenderer` でダーク配色にする
- 新しいコントロール・ダイアログを足すときも、必ずこの配色・ダークテーマを適用してから実装する

## アーキテクチャ（2プロセス構成）

- **エンジン（C# / .NET 10・`engine-cs/`）**: ヘッドレス動作の `KurokoEngine.exe`。
  カメラ取得（OpenCvSharp）→ 髪セグメンテーション（BiSeNet face-parsing ONNX / ONNX Runtime + DirectML）
  → 再着色（HSVブレンド＋色ガイド＋フェザー）→ UnityCapture仮想カメラ出力。
  名前付きパイプ `\\.\pipe\kuroko` でコマンドを受信し、全パラメータを再起動なしにライブ反映する。
  推論と出力をスレッド分離し、推論が18fpsでも出力は約30fpsを保つ。多重起動はMutexで防止。
- **GUI（WPF / .NET 10）**: リッチUI（カラーピッカー・スライダー・プリセット）。名前付きパイプでエンジンを制御。
  プレビューは共有メモリ「KurokoPreview」でエンジンから直接受け取る
  （＝会議相手が見る映像そのもの・WYSIWYG。仮想カメラをOpenCVで開けない問題を回避）。
- 別プロセスにする理由: エンジンがクラッシュしてもGUIが生存する（堅牢性）。ネイティブ依存をGUIから隔離できる。
- ※2026-07-23に商用SDK(C++)ベースの構成から上記のOSS構成へ全面移行した。

### パラメータ棚卸し（全パラメータの分類）

パラメータは3種に分ける。①ライブ変更可（`eval_js`。再起動不要）②要再起動（起動時に確定。変更時はエンジン
プロセスを再起動して反映）③GUIから省く（ほぼ使わない/内部用）。

**① ライブ変更（eval_js。`test_Hair` の `config.js` が公開）**

| GUI項目 | JS/コマンド | 区分 | 実体 |
|---|---|---|---|
| 髪色 | `setColor('#RRGGBB')` → `color` | 基本 | material `hair_color`(vec4) |
| 彩度 | `setSaturation(v)` → `sat` | 基本 | `color_params.y` |
| 明度 | `setBrightness(v)` → `bri` | 基本 | `color_params.z` |
| 色相シフト | `setColorShift(v)` → `shift` | 詳細 | `color_params.x` |
| 検出しきい値（生え際の境界） | `setThreshold(v)` → `threshold` | 詳細 | `filter_params.x`（2026-07-23にシェーダー定数から可変化） |

**② 要再起動（起動時に確定。変更時はエンジンを再起動）**

| GUI項目 | 実体 | 扱い |
|---|---|---|
| カメラ選択 | 起動引数 `camera_index`（OpenCVで開く） | GUIで選択→エンジン再起動 |
| エフェクトON/OFF | 起動引数 `effect_path`（`load_async`は起動時） | 当面は再起動で切替。将来は`load_async`でライブ化検討 |

**③ GUIから省く（ほぼ使わない/内部用）**

- 取り込み解像度（1280x720要求→1920x1080）・仮想カメラ出力解像度/デバイス（固定運用）
- 鏡像/セルフィー（`require_mirroring`。現状true固定。要望あれば基本トグルとしてライブ化可能）
- マスクの時間平滑化 `enableFiltering`（`addFeatureParam(HAIR,...)`。ちらつき低減。まず既定onで固定）
- レンダーバックエンド・FPS上限(30)・ブレンド等の内部状態

**GUIの区切り**: 基本設定＝髪色・彩度・明度／詳細設定＝色相シフト・検出しきい値。数が増える場合も
この2区分を維持する。②の変更はエンジン再起動を伴うため、GUIがエンジンのプロセス寿命を管理する
（起動・再起動・停止）設計とする。

## プリセット

設定を保存・呼び出せるようにする。想定運用は「髪色は"いつもの"お気に入りを固定で使い、
フィルタ系は会議環境（照明・背景）に応じて都度調整する」。そのため**2系統**に分ける。

- **髪色プリセット（color preset）**: 色の見た目を決める値のみ（base色・色相シフト・彩度・明度）。
  お気に入りの色をよく使い回す前提。
- **フルプリセット（full preset）**: 髪色＋フィルタ系パラメータ（検出しきい値・エッジぼかし等、環境依存）
  を含む全設定。

保存先は `%AppData%\Kuroko\presets.json`（ユーザー領域。将来の自動アップデートで消えない場所）。

### デフォルト髪色プリセット（初期同梱）

実際の発色は元の髪の明るさとの合成で変わるため、下記は目安値。ユーザーが微調整・追加・削除できる。

| 名前 | HEX(目安) |
|---|---|
| 黒 | `#1A1512` |
| 金髪 | `#C8A45C` |
| パープル | `#8E5BB5` |
| シルバー | `#BCC0C4` |
| 白 | `#EAE7E1` |
| ミルクティー | `#B08D6A` |
| ピンク | `#DD7BA6` |
| ネイビー | `#2A3A5E` |

## AI支援チューニング（Claudeによる自動調整）

フィルタ調整、特に**生え際・髪と肌/背景の境界**は手動では難しく手間がかかる。ここをClaudeに任せる機能を設ける。

- 動作イメージ:
  1. アプリが処理後フレーム（＋可能なら髪マスク）を数枚キャプチャする。
  2. 画像と現在のパラメータをClaude（画像入力対応モデル）へ送る。
  3. Claudeが境界品質（肌への滲み・背景への誤着色・後れ毛の取りこぼし等）を評価し、
     パラメータの調整案を返す。**生え際は慎重に**扱う。
  4. アプリが調整案を（既存のパイプ/eval_js機構で）適用し、必要なら数回反復する。
  5. 併せて**その後の手動調整の指針**を人間可読のテキストでフィードバックする。
- 前提: 検出しきい値・エッジぼかし等の**セグメンテーション/境界パラメータをランタイムで可変**にする必要がある
  （現状 `hair.frag` のしきい値はシェーダー定数なので、まず露出方法を実装する）。
- 実装時: AnthropicのAPIキーが必要。API/SDKの正しいモデルID・使い方は `claude-api` スキルを参照して実装する。

## 配布・運用（フェーズ3で整備）

- 配布は **GitHub の Release ページ**。インストーラ（例: Inno Setup / MSIX）を用意する。
- **自動アップデート**を可能にする（Release の最新版を検知して更新。例: 独自チェック or Velopack/Squirrel 等）。
- 現時点は**試用フェーズで利用者は作者本人のみ**。ただし将来の配布を見据えた作りにしておく。
- タスクトレイ常駐・Windows起動時の自動開始・仮想カメラへのアクセス検知での処理自動開始（`SendIsReady()` の活用）。

## フェーズ

- [x] フェーズ1: 仮想カメラ出力まで実装（使用可能な状態）。**完了**
- [ ] フェーズ2: リッチGUI（設定調整UI）。エンジンのライブ制御化 → WPF実装 → プレビュー・拡充 → デザイン磨き込み
- [ ] フェーズ3: 常駐化・自動起動・自動アクティベーション・インストーラ・自動アップデート
